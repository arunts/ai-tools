# Changelog

## 0.1.0 — 2026-06-08
- Initial release.
- `analyze-sessions` skill: granularity prompt (overall / project / session), incremental
  snapshots with iterative build-up, and an AI-calibrated "time saved → What-X-faster" estimate.
- `stats.mjs` engine (zero dependencies): `aggregate` / `semantic-plan` / `apply-semantic` / `render`.
- Self-contained gruvbox HTML report — dark + light, follows OS theme, in-report toggle.
- Synthetic demo under `examples/` (`build.mjs`, `sample-report.html`).
